export default function Home() {
  return (
    <div>
      <h1>Mini Outreach Sequencer</h1>
      <p>Take-home assessment. Start with the Contacts page.</p>
      <ul>
        <li>
          <code>/contacts</code> — build the paginated, searchable contacts list +
          create form.
        </li>
        <li>
          <code>/campaigns/[id]</code> — build the campaign detail + per-contact
          message generation.
        </li>
      </ul>
      <p>See <code>docs/TASK.md</code> for the full brief.</p>
    </div>
  );
}
